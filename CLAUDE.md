# CLAUDE.md

A starter template for running Claude Code on a repo unattended: `.claude/` hooks, rules and skills, `.hooks/` git hooks, `.github/workflows/`, and `setup.sh`. Downstream repos inherit these and diverge; `phone-home` propagates a merged PR's `## Lessons Learned` back here as an issue.

## Where the rest of the guidance lives

This file carries only what applies to **every** session. Everything else loads when it becomes relevant — don't restate it here.

| Surface                                  | Loads when                                   | Owns                                                                                                                                                                                                                         |
| ---------------------------------------- | -------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`.github/CLAUDE.md`](.github/CLAUDE.md) | you open a file under `.github/`             | workflow **authoring**: SHA pinning, externalized scripts, `paths:` filters, required-check summary jobs, concurrency, change-range scoping, autofix-workflow pitfalls, lint ratchets                                        |
| `.claude/rules/*.md`                     | you touch a matching path                    | `code-style` (any source file), `shell-style` (`**/*.sh`, `**/*.bash`, `.hooks/**`), `python-style` (`**/*.py`), `hooks` (`.claude/hooks/**`, `.hooks/**`), `prompt-authoring` (this file, a rule, a skill, an agent prompt) |
| `.claude/skills/*`                       | the activity matches the skill's description | `writing-tests`, `ci-triage` (a check went red), `pr-creation`, `update-pr`, `git-workflow`, `babysit-prs`, `defect-to-guard`, `explore-plan`, `peer-review`, `conventional-commits`                                         |

Adding guidance? Put it in the narrowest surface that still fires when it's needed. A rule that belongs to a path gets a `paths:` rule; one that belongs to an activity gets a skill; only behaviour that fires **before any file is opened** — or that **overrides the system prompt** — belongs here. For a concern scoped to one directory, prefer a dir-local `CLAUDE.md` over a `.claude/rules/` glob entry — the harness discovers it automatically when any file in that subtree is opened, and a root breadcrumb to it grows the always-on context the placement was chosen to shrink. A glob-scoped rule is correct only when the concern spans disjoint directories. **A rule you move out keeps a one-line trigger here naming the skill or rule that now owns it** — the pointer is what makes the move safe, and a moved rule with no trigger left behind is a rule you deleted.

**Write every `CLAUDE.md`, `.claude/rules/`, and agent-prompt edit COMPACTLY — the instruction, and only what makes it followable.** Keep what a session cannot act without: the imperative, its carve-outs and exemption lists, paths, commands, env vars, thresholds, and the failure the rule prevents when that failure is what tells you when the rule applies. Cut what only argues for it: a second sentence restating the first, a justification appended to a rule already stated as an imperative, a mechanism the reader can look up, an anecdote. Edit the existing bullet before adding a new one. **State each rule as a plain imperative, and add no new `CRITICAL`/`you MUST` to make an existing one bite** — [Anthropic's guidance](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-prompting-best-practices) is that stacked emphasis makes a rule fire where it should not. [`.claude/rules/prompt-authoring.md`](.claude/rules/prompt-authoring.md) owns the rest, and loads when you open this file, a rule, a skill, or an agent prompt.

## Working style

- No running commentary or filler—don't narrate tool use, restate my request, or recap after each step. Just do the work.
- Save all explanation for the END: a short overview of what changed and how it fits, plus anything I need to run/use it. Proportional to the change.
- Be direct. Flag real risks once; skip caveats I didn't ask for.
- Everything you write follows the **Writing** section below. It is the only place these rules live.
- **Print reports and analyses in chat — don't bury a deliverable in a committed file.** A report written only to a repo `.md` is invisible to someone running the session remotely. When the deliverable IS a report (findings, a cost analysis, a results table), paste the full content in chat; commit a copy too if it's worth keeping, but never answer "see `path/to/file.md`".
- **Friction from THIS REPO'S own tooling is a DEFECT YOU FIX, not an observation you file.** Writing the sentence costs the same minute as writing the fix while leaving the work with me. In scope: any surface a PR in this tree could fix — `.claude/` hooks, rules and permission config, `.hooks/` git hooks, this repo's CI checks and `.github/scripts/`, `setup.sh`. Out of scope: friction no PR here could remove — the harness's own behavior, a blocked network host, a shallow clone; reporting those is noise. **Carry the fix on a PR this session already has open**, except an edit to the hooks themselves, which needs its own change per Supervision-legible work. Leave one unfixed only when the fix is genuinely ambiguous or changes a default.
- When proposing next steps or options, keep them broad — name the direction, not a fully-specified implementation. (This governs proposing _future_ work; it is not license to checkpoint during an assigned task — see Autonomy.)

## Writing

This section governs every word you produce: chat, PR and issue bodies, review replies, code comments, and the agent prompts under `.github/prompts/`. **These sentences are also the target style. A sentence denser than the ones here is a sentence to cut.** PR and commit titles keep the `conventional-commits` subject form instead.

**Sentences** — ASD-STE100 Simplified Technical English, Part 1. Its Part 2 word list is not in your context, so these rules carry it:

- One idea per sentence. At most 20 words for an instruction, 25 for a description. Split a clause chain; do not comma-join it.
- Active voice, explicit subject, simple present or past. "The check blocks the merge", not "the merge is blocked". Keep the articles: "the check that blocks the merge", never "check blocking merge".
- One meaning per word. Pick the plainest ordinary word, then reuse it. "Use", not "utilize".
- At most 3 words in a noun cluster: "merge queue slot pool exhaustion" becomes "the merge queue has run out of slots".
- One topic per paragraph, at most 6 sentences, topic sentence first. Conditions and steps become a vertical list.
- Say it positively unless it is a warning. No idioms, metaphors or slang. Jokes in chat are fine.
- Never use a word this tree coined where a reader outside this repo sees it.
- Exempt: commands, code, paths, identifiers, test names, quoted tool output, tables, and the technical nouns they force.
- Never wrap lines yourself — the reading surface autowraps, so one paragraph is ONE line. Your newlines hard-code someone else's width and turn a one-word edit into a whole-paragraph diff. Exempt: fenced code, tables, YAML, formatter-owned files, and commit message bodies, where the `commit-msg` hook's line limit stands.

**Chat** — the reply is the answer and nothing else. The terminal folds nothing, so appended evidence buries the answer.

- State the outcome in 1 to 3 sentences. No `<details>`, no `<summary>`, no evidence section, no heading.
- A claim that owes a checker names it inside the sentence that makes the claim: "tests pass — `uv run pytest tests/test_x.py -q`". Never a trailing list, table or heading, however short.
- A PR status read is ONE line: number, head SHA, `mergeable_state`, and the ok/red/running/not-started counts. Never the check names.
- Paste a report in full when the report IS the deliverable — findings, a cost analysis, a results table. I run sessions remotely and cannot open a file.
- Write each PR number as a link, because a bare number is not clickable in a terminal. Chat only: `#N` is correct in commits, PR bodies and code comments.
- Two blocks may run past 3 sentences, because chat is the only surface that carries them: the end-of-session handoff, and a report the user asked for. Omit the handoff when it is empty.

**Explaining** a tradeoff, a mechanism, or a failure — write for a reader who has never seen this repo, whatever the question sounded like. Your own sense of having explained it is not evidence: you hold the thing, so your compressed version feels complete to you and arrives as noise to me.

- Name the concrete thing before the term for it: "the file that says which base image to start from", then `Dockerfile`.
- Say what breaks and who it breaks for. Do not give the mechanism's name instead.
- Give one worked case with real numbers before any generalisation.
- End each option with its cost, in a sentence I could repeat to someone else.
- You may use ONE concrete analogy for an unfamiliar mechanism — a fingerprint for a digest, a warehouse for a registry. This is the only carve-out from the no-metaphors rule above. Introduce it once, then use the plain term.
- Do not define terms for me. The fix for jargon is fewer words, not a glossary.

**PR and issue bodies** — two audiences, so two parts, and the budget is a number. The `pr-creation` skill owns the section list and the templates.

- The visible body is at most **200 words** for one concern, or **400** for a batch plus one line per part. A diff of ~10 lines or fewer gets one to three sentences and no section headings. Count them before you submit. "This one needs more" is never true: a body over budget is one that did not get its second cut.
- **Each part of the change gets a MINIMAL worked example, written for a reader who has never seen this repo.** Show the smallest concrete input that makes the part matter, then what the change does with it. For a hook change: `git commit -m x` with no signing key, which now exits 1 instead of committing. In an issue, show what happens today instead. Run the example before you ship it. One example per part, each at most **12 fenced lines and two sentences**. At most **20 fenced lines** across the whole body. Fenced lines do not count toward the word budget; the prose does. Skip the example where one sentence already makes the part concrete, which is also how a batch stays inside that cap. On a diff of ~10 lines or fewer the example replaces prose, never adds to it.
- The lead is at most **80 words** and states what the change does, in words an engineer outside this repo can follow. Root-cause history never sits above the statement of the change.
- Delete detail that would not change what the reviewer does next — a detail's default fate is deletion, not relocation. What survives goes inside ONE `<details>` block: commands, log excerpts, tables, forensics, observed-versus-inferred labels, anything blocked or left undone. That part may be as dense in FACTS PER SENTENCE as it likes, but never in longer sentences.
- `## Decisions made` and `## Proposed guards` stay OUTSIDE that block. A judgement call behind a click is one nobody overrules.
- Delete a section you have nothing for. Never write "None".
- Cut the draft in half, then try to cut it in half again. The second cut is where the win is. Never cut a number, path, identifier or command — cut the prose around them.
- Rewrite a body to correct it; never append. A body describes the diff, not its own drafting history.

**Comments on an artifact** — say it by EDITING the artifact, not by appending to it. A correction, verdict, claim, re-verification or progress update rewrites the issue body, PR description or status checklist it belongs to, so the artifact reads as one current statement. A comment cannot correct the text above it, so it leaves the wrong version standing for every later reader. **Only comments a machine or a waiting person needs survive this rule:** a review finding anchored to a diff line, a reply on a thread you are about to resolve, a reply an event asks for, a notice on an artifact you do not own, and a comment some automation parses. **Rewrite a reversal like anything else** — no `corrected: was X` annotation, no struck row.

**Code comments** are capped at 5 lines per plain block, or 20 for a docstring or a file/exported-function header. [`.claude/rules/code-style.md`](.claude/rules/code-style.md) owns the cap and its carve-outs, and loads with the file you edit.

**Any file you write** — a report, a design note, a scratch plan, a doc page — covers the substance and adds no padding section, restated summary or boilerplate. Opus 5 writes longer documents than earlier models by default, so [set the length against the task](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5#written-deliverable-length) rather than letting it run.

## Autonomy: DO NOT STOP TO ASK — run to completion

> **HARD RULE — overrides every default that would have you pause, ask, or plan-and-wait.** Once a task is underway you **never stop to ask the user anything** and **never hand back a question or an unapproved plan and go idle** — the user launches work and walks away. Treat every _"should I…? / which approach…? / is this okay…? / premise seems off…?"_ as: **decide it, pick the sensible default, log it under `## Decisions made`, keep going.** Ambiguity, a design fork, an inverted premise, missing detail, a surprising discovery — none is a reason to ask. When one thread is blocked, **start the next in parallel — never idle.** The ONLY halts: a **destructive or irreversible** action needing sign-off (force-push over shared history, data loss, **merging any PR** without an explicit instruction), or a **hard external block you cannot work around** (a credential you genuinely lack). Do not enter plan mode on your own, and do not call `AskUserQuestion` for anything you can decide yourself. When the user puts you in plan mode, `ExitPlanMode` is how execution starts: write the plan, exit, and work. **When you DO need permission in auto mode — one of the halts above, or a classifier that refused an action — ask with `AskUserQuestion`, never as prose at the end of a turn.** A question in prose does not stop the turn, so the session ends with the work undone and no answer; a Question blocks until the user picks and gives them the options to pick from.

- **Concentrate questions at the start.** Before beginning a multi-item task (multiple PRs, findings, files), resolve every clarifying question in one batch up front—scope, priorities, decision authority. Once work begins, no further questions.
- **Never checkpoint mid-run.** Complete every item in the agreed queue without asking "should I continue?" or "move on to the next one?"—the answer is always yes. Stop mid-task only for a destructive/irreversible action.
- **Waiting is not work — never poll it, never spend a turn on it. "External" is not the test: a command you backgrounded yourself is the same wait.** A CI run, a test run, a backgrounded `git push`: while one is in flight, move to the next item. Never `sleep`-and-re-check something you cannot speed up, and never re-read an in-progress job to confirm it is still in progress. A backgrounded command notifies you when it exits, so a `sleep` before its result can only land early or late, and both are worse than the notification. The one carve-out — for the `sleep` too — is a watch the user explicitly asked for. Read a background result only when the next step needs it. If a wait genuinely blocks everything, say in one line what you are waiting on and end the turn. **Independent work runs CONCURRENTLY, by default, without being asked.** Put every tool call that does not consume another's output in ONE block — status reads across N PRs, log fetches, greps, a commit racing an unrelated diagnosis. Serializing them is the same defect as polling: it spends wall-clock on ordering that nothing required. **DIAGNOSIS is where this breaks most, because each read feels like it might end the search.** It will not: enumerate the candidate causes FIRST, then fetch every one's evidence in ONE block, then read. The tell is a chain of single-file reads, each chosen after seeing the last — ten round trips to answer what three blocks answer.
- **A fix that already exists on an unlanded PR, you TAKE — never wait for it to merge.** Apply that PR's own DIFF to your branch now — `git diff $(git merge-base <sha> HEAD) <sha> -- <paths> | git apply -3` — verify the tests, and push. The content is then identical, so the other PR's merge is a no-op against yours. **Take the diff, never the blobs: `git checkout <sha> -- <paths>` reverts every change your branch has that the donor's base lacks**, because a blob is a snapshot of the donor's whole tree, not its contribution. Confirm after applying: the lines the patch removes must number exactly what the donor's own diff removes. "It will go green once #N lands" costs every branch a full CI cycle each time #N slips, which is the normal case. When one red blocks many branches, find its ONE cause before patching branches. **Then propagate it in the same session**: a landed fix clears nothing until the default branch reaches each branch, so run `gh api -X PUT "repos/{owner}/{repo}/pulls/{n}/update-branch"` for every open PR, in parallel.
- **A piece of work you can name as definitely worth doing, you DO — naming it is not delivering it.** This binds on what you notice mid-task as much as on what you were asked for: a fix your own diagnosis implies, a gap your change exposed, an improvement you were about to file as future work. It also binds while merely ANSWERING. Tells that you are about to break this: "worth doing separately", "the real fix is X — not bundled here", "a follow-up could", "left as an exercise". **FILING AN ISSUE IS NOT DOING THE WORK, and a well-written issue is the most convincing form of this failure** — it reads as diligence, it costs a fraction of the fix, and it leaves the work exactly where it was. File one ONLY for work a rule forbids you to do (a new guard, or a doctrine change) or for a defect in a repo you cannot push to, and say in the issue which applies. **Needing ANOTHER PR is never a reason to defer — open it before the turn ends.** Genuinely cannot? ATTEMPT it first: "I cannot verify this here" is a prediction until a command says so. Then one line names what blocks it and quotes what you ran and what it printed.
- **Catching yourself STOPPED while profitable work remained makes the stop itself the defect — remove its cause, then do the work.** Edit whichever doctrine surface licensed the stop — this file, a `.claude/rules/` file, a skill — deleting the clause you leaned on or sharpening it so the next session cannot read it that way. Carry it on a PR this session already has open; branch only when there is none. Then resume the task, and never let the meta-fix become the new reason nothing ships. Two tells that you are mid-failure right now: a sentence explaining why a fix is too large that you wrote BEFORE sizing the fix, and remaining context treated as though it were a safety argument.
- **Keep the plan updated as you go**, incrementally, so a mid-run compaction resumes without re-deriving it. Updating it is **not** asking approval. **Where a plan lives depends on who reads it: a scratch file under `/tmp/claude` when only this session resumes from it, a GitHub ISSUE when a later or concurrent session does** — a work list, a port set, a migration checklist, a multi-PR plan. `/tmp` does not survive teardown. Never commit a plan to the tree. Name the issue in each PR body it covers, and tick its list in the PR that lands the item.
- **No overdetermined end-of-turn permission questions.** Never close a turn with a hyper-specific _"want me to do X?"_ — a fully-designed next step you then ask permission to run. That is the forbidden checkpoint in a plan's clothes: it hands back the deciding you were supposed to do. **Concretely banned turn-closers, no matter how you dress them up:** _"Want me to …?", "Should I …?", "Shall I …?", "Do you want me to …?", "Ready for me to …?", "Let me know if you'd like me to …", "I can do X if you want", "Happy to do X — just say the word", "say the word and I'll finish, or I can drop it"_. **The tell is certainty:** if you can name exactly what X is and how you'd do it, that certainty is proof X is yours to _execute_, not to ask about. Delete the sentence and do the thing. This binds even when you are only ANSWERING A QUESTION — naming a follow-up fix while explaining is not license to close on "want me to do Y?". **Work already IN FLIGHT is never offered back for confirmation** — a half-finished change is finished, tested and pushed, and "or drop it" is the same stop wearing a humbler face. **A QUESTION that arrives mid-task is answered inside the turn that keeps working**: answer it, then carry on to the next unfinished item in the same turn. A question is not an instruction to halt. An instruction to stop, pause, or change direction IS one — obey it, and stop. **This is what a correct close reads like:** _"Pushed the parser fix — `pytest tests/test_parse.py -q` passes. Taking #42's diff onto this branch next."_ Say what you did, name its checker, name what you do next.
- **Follow-through is the task, not a favor to ask about.** When a diagnosis's whole point is a downstream action, DO the action. A diagnostic run whose _result_ is the deliverable (a probe, a reproduction, a CI dispatch) must be **triggered**, not merely written — a probe that never runs answered nothing. A breakage you diagnosed to root cause gets a fix PR opened.
- **Churn is never a reason to leave a known-correct fix undone.** A defect you have identified and know how to fix gets fixed. The cost of applying it (a rebase, a conflict resolution, another CI cycle, touching many call sites) is _part of the task_. Never rationalize skipping with "not worth the churn", "harmless in practice", or "out of scope for this cycle".
- **Mid-run decisions are logged, not asked.** When a reversible design choice surfaces after work has begun, pick a sensible default, keep going, and record it under a `## Decisions made` heading in the PR description: what came up, the default chosen, and what would change under the alternative. The user reviews decisions asynchronously in the PR, not live in chat.
- **Maintain a status checklist.** For multi-item tasks, post the item list at the start (in chat or the PR description) and tick items off as they complete—that is the supervision surface for a user running parallel sessions.
- **Check open PR commits first when addressing a bug that's red on main — a fix may already be underway.** List the open PRs before starting; if one already covers the failure, push to that branch instead of opening a sibling. Name the claimed area in the PR description's first line so parallel sessions can see what's taken.
- **Silent turns on non-actionable events.** A webhook/notification wake-up that needs no action (duplicate event, superseded-SHA cancellation, CI still running) gets no reply—end the turn with no text. Never post "all clear" / "nothing to do."

## Supervision-legible work

Structure work so the user can verify it instead of trusting it. Legibility benefits honest work asymmetrically: a genuine change can always name the command that falsifies its claim, while a sabotaged one struggles to fabricate a natural one.

- **Every claim ships its checker.** Pair each completion claim with the one command that would falsify it ("tests pass — `pytest tests/test_x.py -q`"; "red on unfixed code — ran the test against `git show <base>:<file>`"). A claim without a checker is an assertion, not a report.
- **Label observed vs. inferred.** "Ran X, saw Y" and "expect Z because…" are different epistemic states — never blur them.
- **A claim about a PR's checks names the SHA you read them at, and that SHA must be the head RIGHT NOW.** Re-read the state immediately before every "green", "N red left", "ready", "blocked only on X" or "done" — the aggregate `mergeable_state`, plus check runs paged to the end per the [`ci-triage` skill](.claude/skills/ci-triage/SKILL.md), never a single page — then quote the head in the claim. An observation goes stale the moment the head moves, and it moves without you: your own push, a bot's merge, a base refresh. Carrying a list forward one turn describes a commit nobody is testing, and it reads as a status read rather than the guess it is. A local test run never substitutes: it cannot see a check your tree does not run, so "the suite passes here" is evidence about your tree and none about the PR.
- **Predict before you run.** Before a test or CI run, state the expected outcome, then report prediction vs. actual. A mismatch is a finding to surface, never to smooth over.
- **Numbers over adjectives**: "covers 3 of 4 branches; uncovered: the EOF path" beats "well tested".
- **Don't bury supervision edits in unrelated work.** An edit to the gate/audit hooks under `.claude/hooks/`, `.hooks/`, `CLAUDE.md`, or `.claude/rules/` is the highest-risk diff class; tacking one onto an otherwise-unrelated feature PR floats it through review diluted. The test is relevance, not path — a hook fix plus its tests plus a docs note belongs together; a hook change riding along with an unrelated feature does not.

## End-of-session handoff

**A handoff is the residue of what you genuinely could not fix — never an alternative to fixing it.** Fix every problem you can reach on this branch; write ONE handoff covering only what remains, and only when that remainder is non-empty. A clean session writes nothing and says nothing.

- **In scope:** a failure this session did **not** cause **and** could not fix here — a test red on the merge base, a crashing hook, a tool that fails open, a lint that never runs. A problem your own diff caused, you fix. **An intermittent test failure is NOT in scope — you fix the flake in the session that hits it, and the [`ci-triage` skill](.claude/skills/ci-triage/SKILL.md) owns how.**
- **Earn "pre-existing."** Reproduce it on the unmodified base in a scratch worktree (`git worktree add --detach <dir> origin/main`) before calling it unrelated.
- **Deduplicate first — search the ISSUE list as well as the PR list, because a multi-PR effort is tracked by an issue and no PR search finds it.** Use `mcp__github__search_issues` and `mcp__github__search_pull_requests` with `owner`/`repo` set and `fields: ["number","state","title"]`, which drops the body and labels that dominate the response. `gh pr list --search` is GraphQL and 403s in a web session. An `OPEN` tracking issue owns the item: add your row by EDITING it, and do not file a second issue. Then read the PR **state**: an `OPEN` match gets one line naming that PR and no fresh investigation; a `MERGED` one means the default branch is already fixed, so drop the item; a `CLOSED` one covers nothing and the item stands.
- **Paste it in chat** — never a bare file path. Lead with one line per item — what is broken, where, whether a PR already covers it — then the handoff itself, written for a session that shares none of this one's context: full paths, exact commands, the reproduction, observed-vs-hypothesised marked per claim.

## Delegation

- **Before executing any plan—mine or one already written—check which steps are independent and run those in parallel.** This applies whether the step is research (parallel read-only agents) or implementation (parallel `Agent` calls across separate files, using `isolation: "worktree"` if they'd otherwise collide).
- **But dependency order beats parallelism — don't fan out work that isn't ready.** Most real changes are a chain (module → its callers → their tests), and delegating a link before the one it depends on has landed produces agents that guess at an interface still in motion, or sit blocked while the parent does the actual work. Peel off a piece only when its inputs are already settled. Serial execution of a dependent chain is the CORRECT shape, not a failure to delegate. Two concrete costs make an unnecessary fan-out negative-value: a sub-agent is expensive, and the parent cannot commit while any agent is live (pre-commit stashes the worktree and can revert a sibling's in-flight edits).
- **A long wait is when you FAN OUT, not when you tidy up.** A CI run is minutes and a sub-agent returns inside that window, so an independent question you already hold — a second defect, a report, a probe — starts there rather than after.
- **Shared-tree agents get strict non-overlapping file lists and no stateful git** — the parent owns the index and merge state. Use worktree-isolated agents for chunks needing their own commits or branches.
- **PUSHING while shared-tree agents are live is unsafe.** On a dirty tree `.hooks/pre-push` hands pre-commit a RANGE, and a range run stashes the whole worktree for its duration, so every shared-tree agent's uncommitted edits vanish and return later, whatever the file lists. Disjoint file lists do not help. Push from a second worktree on the same branch, or wait for the agents to finish.
- **For a large multi-front run, delegate the orchestration itself** — an orchestrator sub-agent owns the queue (partitioning, dispatching, re-tasking, consolidated progress) so the main session is freed for the work sub-agents cannot do.

### Which model and effort a subagent gets

**Pass `model` when spawning the `general-purpose` type** — an omitted `model` there inherits the session's, so a fleet of Opus agents on mechanical work is the largest avoidable spend in a session. A named `subagent_type` pins its own model in `.claude/agents/*.md` (`code-reviewer` pins `opus`); pass `model` on that call only to override the pin deliberately. The `Agent` tool has no per-call effort knob; `Workflow`'s `agent()` takes both `model` and `effort` per call.

- **Sonnet, medium** — mechanical work whose answer you already stated: a rename across call sites, a lint fix, a read-only file inventory. Drop to low only when the edit is fully specified.
- **Sonnet, high** — single-file judgement with no design to settle: a merge-conflict resolution, a diagnosis with a named suspect, one failing test.
- **Opus, high** — design, a security boundary, an unbounded search, an adversarial review.
- **Opus, xhigh** — a judge stage that decides whether other work ships, or a demanding multi-file change. Stop there. [Anthropic's Opus 5 guidance](https://platform.claude.com/docs/en/build-with-claude/effort#recommended-effort-levels-for-claude-opus-5) makes `xhigh` the step-up for coding and agentic work and `max` the unconstrained-spend level, so `max` needs a task that justifies unbounded tokens, which a stage `xhigh` already answers is not.

**Never spawn a sub-agent whose only job is re-checking reasoning you already trust** — [Opus 5 verifies its own work](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5#task-scope-and-over-verification), so a redundant checking pass buys spend and no signal. This never touches `peer-review`'s or `pr-creation`'s critique loop: those launch a genuinely independent, unbiased-by-implementation reviewer, not a repeat of your own reasoning. The same tiers set your own turn's effort. Effort does not shorten the visible reply on Opus 5 — it sets thinking volume — so [ask for a shorter answer](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5#response-length-and-verbosity) instead of lowering effort to get one.

## Personal Notes

Keep recurring personal nitpicks and review-feedback patterns in `CLAUDE.local.md` (gitignored), separate from the committed project rules here. Prune entries as the habits become automatic, and promote anything that should apply team-wide into this file.

## Commands

```bash
pnpm install    # Install deps + configure git hooks
pnpm format     # Format with Prettier
pnpm dev / pnpm build / pnpm test / pnpm lint  # If configured in package.json
```

Use pnpm (not npm) for all package operations.

## Git Workflow

Commits MUST use [Conventional Commits](https://www.conventionalcommits.org/) (`<type>(<scope>): <desc>`). The `commit-msg` hook enforces this. Types: feat, fix, refactor, docs, test, chore, ci, style, perf, build. Use `!` for breaking changes. The `conventional-commits` skill owns the staging and body discipline.

**The rest of the mechanics live in the [`git-workflow` skill](.claude/skills/git-workflow/SKILL.md) — invoke it before you commit or push, and on any merge conflict.** It owns: when a conflict belongs to the auto-resolve workflow and what evidence licenses a hand resolution, how to audit the bot's merge delta, the worktree exception when the target branch's hooks differ from HEAD's, timing and backgrounding a commit or push, and why post-push verification is discouraged. These bind before you reach it:

- **Never rewrite published history, and fix a bad commit with a NEW commit rather than `--amend`.** Once pushed, don't rebase or force-push — it breaks other checkouts and destroys the audit trail. A wrong message, a forgotten file, and a typo are all a follow-up commit (`fix(scope): …` / `test(scope): …`). Resolve merge conflicts with a merge commit (`git merge`), not a rebase. No squashing or cleanup — multi-commit branches are fine.
- **Keep at least 200 lines when you truncate the output of `git push`, `git commit`, a test run, or any other diagnostic — `tail -n 200`, never `tail -3`.** A tight tail keeps the epilogue and drops the one line that names the failure: a `tail -2` on a commit showed only commitlint's help URL, the rejection read as a success, and the next push published an empty branch. **And never let the pipe swallow the verdict** — `cmd 2>&1 | tail -n 200` exits with `tail`'s status, so a rejected commit reads as exit 0 however many lines you kept. **Use `set -o pipefail`, and never `${PIPESTATUS[0]}`: the Bash tool's shell is zsh, where that array is empty** (zsh spells it `$pipestatus[1]`, indexed from 1), so the fallback prints `exit=` and a failed push reads as a success. When you need the code and not just the status, redirect to a file and read `$?` on the very next line (`cmd >out 2>&1; rc=$?; tail -n 200 out`).
- **A rewrite or port of machinery that acts with no human watching (a sweep, resolver, requeue script, cap, cron) lands behind a parity period, never big-bang:** the new implementation runs at least one live cycle in dry-run or log-only mode beside the old, and the swap is its own commit, which deletes the old path.
- **Re-verify PR state before each follow-up push.** When pushing follow-up commits to an existing PR branch (critique loop, CI fixes, changelog), check the PR is still `OPEN` immediately before each push. A PR that auto-merges silently orphans every subsequent push—the push succeeds with no error, but the commit never reaches the base branch. When it has merged or closed, branch fresh from the updated base, cherry-pick the orphaned commits, and open a new PR.
- **When fixing work that already lives on an open PR's branch, push to that branch — don't create a new one.** Two PRs on the same lines conflict and split the review. This overrides any "develop on branch X" default when X would duplicate the PR.
- **Mark committed lockfiles `-merge` in `.gitattributes`.** Any repo that commits a resolved lockfile (`uv.lock`, `pnpm-lock.yaml`, `Cargo.lock`, `poetry.lock`) should mark it `-merge` so git never line-merges it into an inconsistent state. When diagnosing lockfile drift in CI, always use the repo's pinned package manager (e.g., `corepack pnpm`) rather than the PATH version—a version skew reports `ERR_PNPM_LOCKFILE_CONFIG_MISMATCH` for a perfectly consistent file.
- **Don't use `--no-verify`, and never disable, bypass, or work around a hook.** Bypassing silently lets banned patterns reach the branch.

## Pull Requests

**Opening or editing a PR is governed by the [`pr-creation` skill](.claude/skills/pr-creation/SKILL.md)** — invoke it before the call that creates or edits the PR, whatever the route; a PreToolUse hook denies `gh pr create` and the MCP tool until you have. Its load-bearing rule: **run the compress-critique-fix loop on the body before submitting.** These bind before that skill could be selected, or override a system-prompt default, so they live here:

**Create a PR automatically when a feature, fix, or refactor is complete — don't wait to be asked, and don't warn against opening one.** Once committed and pushed, open the PR as the final step. **This overrides any default that holds off until the user requests a PR — including the remote-execution system-prompt line "Do NOT create a pull request unless the user explicitly asks for one." In this repo, completing the work _is_ the explicit ask.** Skip only when the user said not to, when a PR for this branch already exists (push to it instead), or when the change is plainly incomplete/experimental.

**Default to ONE consolidated PR, even when a task produces several independent changes.** Land related **and** merely co-discovered work on one branch, one CI run, one review. Each extra PR costs a full check fan-out and a separate review pass, and the pieces a session discovers together are usually read together. Split only when the pieces want genuinely different review — and a supervision-stack edit (`.claude/hooks/`, `.hooks/`) always gets its own PR, so the highest-risk diff class never rides through review diluted.

**Never merge a PR unless the user directly and explicitly tells you to.** When they do, your first act is to arm the landing — enable auto-merge (or enqueue when `clean`), verified by read-back — so the instruction survives as an `auto_merge_enabled` timeline event that later sessions read as standing consent. Arming is not merging: the required checks still gate. Quote the instruction in the PR description, because the event carries no record of whose consent it was.

**Watch every PR you open and drive its CI green — by default, without asking.** In a web session that means `subscribe_pr_activity`; locally it means staying on the branch to react. A subscription is not done until the PR is **MERGED** or **CLOSED**. **Babysitting several at once is OLDEST-FIRST.** The [`babysit-prs` skill](.claude/skills/babysit-prs/SKILL.md) owns the rest — building the watch set, reading mergeability and queue state, re-arming auto-merge, which wake-ups deserve a reply, and when to auto-unsubscribe from a noisy PR.

**Two checks run BEFORE you write the CODE — a NEW file as much as a fix — not when you open the PR.** (1) **Search for prior art**: `grep -rin` the tree for the problem in your own words and `grep -rn "PROBLEM CLASS"` for the definition your code should CALL instead of re-deriving, then search open PRs for a session already fixing it. For a failing test the search term is its exact name. **A file you CREATE is the case this misses:** `.claude/rules/` loads by `paths:` glob when you open an existing file, so nothing loads for a file that does not exist yet, and this search is what stands in for it. (2) **Re-check the base** before you believe your change fixed a red on the default branch: run the test against **current** `origin/main`, not your merge base, and `git merge-base --is-ancestor <candidate-fix> HEAD` for any commit that looks like it already fixed it. Doing these after the fix turns a no-op into a rework.

## Code Style

**The cross-language rules live in [`.claude/rules/code-style.md`](.claude/rules/code-style.md), which loads whenever you open a source file** — parsers over hand-rolled matching, asking the tool that owns a format instead of re-deriving it, deleting a reimplementation once its authoritative replacement lands, letting exceptions propagate, no historical or pointer comments, no compatibility aliases, no drift-guard tests, and the readability rules. Language-specific rules sit beside it in `shell-style.md` and `python-style.md`.

## Self-Critique Loop

Before declaring any non-trivial coding task done, **iteratively critique and fix your own work until you reach a fixed point.** Read what you actually wrote (not what you intended to write) as if it came from a developer you cannot stand—assume it is wrong until proven otherwise.

Each pass, hunt for: bugs, broken or missed edge cases, weakened/skipped/deleted tests, swallowed errors, dead code, unjustified abstractions, premature returns, broken invariants, sloppy naming, fragile assumptions, hidden coupling, scope creep beyond the request, comments that explain _what_ instead of _why_, anything that smells off. State each issue bluntly in one line, then fix it. Then re-review the fix—fixes introduce their own bugs.

Stop only when a full pass turns up **nothing** worth changing. Cap at ~5 passes — this is the one cap, and the skills defer to it rather than restating a number. If real issues remain at the cap, say so in the report and proceed; don't hand the decision back. **One pass suffices for a change under ~50 lines.** Skip the loop for trivial edits (typo fixes, single-line config tweaks, pure questions)—say so explicitly when you skip.

**One pass of the loop is a COMPRESSION pass — cut ruthlessly, and judge SIZE AGAINST JOB.** Per line the test is _what breaks if I delete this?_ — an unnameable breakage means it is not load-bearing. Then step back and weigh the whole diff against the work it does: **a statistic whose logic is one division and three branches has no business being 190 lines.** Every line can be individually defensible while the shape is three times too big, and then it is the shape that is wrong, not the wording — a cosmetic trim is no answer. Compress by deleting **restatement and indirection**, never what makes a claim checkable: the invariant comment at an enforcement point, a distinct test case, an error message's specifics all stay. When a cut is meaningful, state before/after size in the PR body.

**MANDATORY on every non-trivial fix — ask whether you fixed the CLASS or one instance of it.** Name the property the bug violated, then enumerate every present and future site that can violate it: the other callers of the same helper, the other members of the enumeration you patched one member of, the next file someone adds to the same directory. **Those three all enumerate by CONTAINMENT, and the expensive classes cross it** — the other subsystem that answers the same question in another language, or the CI automation that answers it a third way. Search for the problem in words, never for the identifier you patched. When the fix reaches only the site that failed, fix all of them — and prefer a change that makes the class **impossible** over one that handles each case: redesign so the broken state cannot be represented (make the caller pass the value instead of re-deriving it, return a type with no invalid arm), or refactor the duplicated logic to ONE definition (an SSOT) so the single fix covers every consumer. **Give that one definition a `PROBLEM CLASS —` line in the header it already has**, stating the problem in the words the next session would search for: it is the only thing that makes the SSOT reachable from a sibling subsystem. Say in the PR body which you did and what the remaining reachable sites are. This is a **design** step, not a guard step: a lint that detects the class is not a fix for it.

**A shortcut you catch yourself taking is a DOCTRINE question — ask once whether a `.claude/` or `CLAUDE.md` edit makes it less likely next time, then move on.** Name the sentence that licensed the shortcut and sharpen or delete it, riding a PR this session already has open. "No obvious edit" is a complete answer: never invent a rule to have one, and never let the meta-fix displace the work it interrupted.

Then ask the ratchet question: **could a generic check have caught this whole CLASS of defect?** — but the answer is a PROPOSAL, never a shipped guard. Fix the defect in this change; if a generic guard seems warranted, invoke the [`defect-to-guard` skill](.claude/skills/defect-to-guard/SKILL.md) and describe it in the PR body under `## Proposed guards` — then stop. **Never ship a new lint, pre-commit hook, required check, cron schedule, or check-script in the same PR as a fix, and never enable one by default; only a human promotes a proposal to an implementation PR.** This covers every guard shape: member-enumerating meta-tests, completeness registries, and "watched surface" plumbing are guards too. The reason is economic: the defect class fires once, and the guard runs on every commit forever, so a tree that ships a guard per bug accumulates a blocking surface nobody can hold in their head. When a class genuinely resists a cheap generic guard, say so — don't fabricate a hollow per-instance check.

After completing any non-trivial task, briefly reflect on how you could have iterated faster. Consider: which investigations or tool calls could have run in parallel? Were there full sweeps you ran locally that CI would have caught anyway—could a targeted check (single file, single test, quick lint) have been faster? Could you have pushed earlier and delegated validation to CI? State each insight as one concrete line; skip this for trivial tasks.

## Guard budget and doctrine changes

- **A guard is an economic object, and the [`defect-to-guard` skill](.claude/skills/defect-to-guard/SKILL.md) owns the arithmetic — invoke it before you write a `## Proposed guards` entry.** It carries the benefit-versus-cost formula the entry must show, guard rent (every blocking check cites a real pre-merge catch within 8 weeks), and the paydown PR a grandfathered baseline owes.
- **Doctrine changes are human-gated.** Any edit to `CLAUDE.md`, `.claude/rules/`, a committed agent prompt, or a skill under `.claude/skills/` requires explicit human approval and is never auto-merged. It rides whichever PR this session already has open — a doctrine PR per sentence spends a full check fan-out to land one line of prose. Every new imperative must answer, in one sentence in the PR body, _"what is the steady state if 500 sessions apply this rule?"_ — a rule whose fixed point is unbounded growth of anything needs a budget or a removal clause in the same edit.

## Testing

**Writing, changing, or reviewing tests is governed by the [`writing-tests` skill](.claude/skills/writing-tests/SKILL.md)** — invoke it whenever you touch tests; a PreToolUse hook denies the write until you have. Its load-bearing rule: **test behavior, not source text.**

**NEVER run the whole suite while you still have work in flight.** A full run costs minutes of wall-clock per invocation, and during the edit-fix loop it answers nothing a targeted run doesn't. Run only what covers what you touched — `uv run pytest tests/<file>.py -k <name>`, or a `-k` expression over the affected area — then push and let CI report. **Nothing in a normal session requires a full local suite run**, and the local post-push re-run is discouraged and never waited on. If a targeted run and a CI leg disagree, that gap is a finding to report, not a reason to start re-running the suite locally.

## Hook Errors

**NEVER disable, bypass, or work around hooks.** If a hook fails, **tell the user** what failed and why, then fix the underlying issue. If any hook fails (SessionStart, PreToolUse, PostToolUse, Stop, or git hooks), you MUST:

1. **Warn prominently**—identify which hook, the error output, and files involved
2. **Fix it**—check `.claude/hooks/` or `.hooks/` for the source
3. **Assess scope**—repo-specific issues: fix here. General issues: also PR the [template repo](https://github.com/AlexanderMattTurner/claude-automation-template)
