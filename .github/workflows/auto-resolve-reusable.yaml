# Auto-resolve ONE PR's merge conflicts with its base branch: a deterministic
# pre-pass (`pnpm resolve-generated`, which re-derives every conflicted DERIVED
# file — a generated artifact via its generator, a lockfile via its own lock
# command) plus an LLM pass over the remaining SOURCE conflicts, pushed back as
# a normal merge commit. A derived file whose source also conflicted is excluded
# from the LLM prompt and re-derived in finalize once the sources are resolved;
# only a conflict with no deterministic resolution AND no textual one (a binary,
# or a `-merge` file owned by no rule) fails the run with a PR comment BEFORE any
# LLM cost. Safety rails:
# same-repo PRs only (a fork's token is read-only and its author is untrusted),
# no WIP drafts — a draft is skipped unless it carries the `approved` label,
# which marks a ready PR the cap (cap-ready-prs.yaml) parked as a draft
# (bot-authored PRs included — the resolved head is re-validated
# by CI + human review before merge), conflicts in protected paths (security boundary,
# launch path, this CI machinery) are still resolved by the LLM but flagged for
# human review in the pushed-resolution comment, and the resolved head is
# re-validated by CI + human review before it can merge. The workflow comments
# on a PR only when it actually pushes something (or fails mid-resolution) — a
# run that ends up doing nothing says nothing. A merge that changes files under
# .github/workflows/ is pushed with the workflow-scoped TEMPLATE_SYNC_TOKEN_ORG
# (the only available token GitHub allows to update workflow files); if that
# secret is absent and the push is refused, the PR is labeled
# `auto-resolve-blocked` and skipped until a human removes the label — otherwise
# every base push would re-run a paid resolve into the same rejection.
# It mutates the PR branch, so — like the pre-commit autofix — it is NOT a
# required check.
#
# Kept a SEPARATE file from the claude-review.yaml reviewers on purpose: the
# `resolve` job below checks out the PR HEAD to merge into it, whereas the
# reviewers run under pull_request_target with base-only checkouts. Co-locating a
# head checkout with pull_request_target trips CodeQL's untrusted-checkout query,
# which is file-scoped and cannot see the per-job event gating — and it would
# blind that query to the secrets-bearing reviewer jobs. Splitting keeps the
# reviewers under full CodeQL coverage and this head-checkout resolver on its own
# unprivileged trigger.
#
# ── Trust model ───────────────────────────────────────────────────────────────
#
# TWO JOBS, and the split IS the security boundary. `resolve` checks out the PULL
# REQUEST'S OWN HEAD and runs things out of it — the `setup-base-env` local
# composite (a `uses: ./…` path resolves against the workspace, i.e. against the
# PR's copy), `pnpm resolve-generated` (the PR's own node code), and the model
# itself. `land` holds every write credential and runs none of that.
#
# The separation is load-bearing rather than tidy, because a single job leaks
# through every step-to-step channel the runner provides. Anything executing in
# `resolve` can append to `$GITHUB_ENV` — `BASH_ENV=/tmp/x.sh` makes the next
# `run:` step source attacker shell before its first line, and `GIT_SSL_NO_VERIFY=1`
# plus `https_proxy=` defeats a later push's `-c http.sslVerify=true`, since the
# variable beats config and a config-FILE audit cannot see it. It can append to
# `$GITHUB_PATH`, or plant a `git`/`gh` in a PATH directory the toolchain step
# publishes. So the unit of exposure is the JOB, not the step: staging the
# resolver scripts from the default branch stops a PR from rewriting the
# resolver's LOGIC, but it was never a boundary around the resolver's
# CREDENTIALS. No such channel crosses a job boundary.
#
# `resolve` therefore holds `contents: read` and the Claude billing tokens, and
# nothing else — the org PATs (AUTOFIX_TOKEN_ORG, and the workflow-scoped
# TEMPLATE_SYNC_TOKEN_ORG, the widest credential in the system) exist only on
# `land`. The residual it keeps is stated rather than hidden: `pull-requests:
# write` on this repository's own GITHUB_TOKEN, so the pre-LLM handoff and the
# failure diagnoses still reach the PR they are about, and the Claude OAuth
# tokens, which are billing credentials and cannot be moved off the job that
# runs the model.
#
# One artifact crosses the boundary — a git bundle of the merge commit, and
# nothing else. `land` treats it as UNTRUSTED: it requires both parents to be
# commits already on the two named branches, refuses conflict markers, and
# replays the same merge in a tree the model never touched to derive what the
# resolution changed. It transmits no manifest for `land` to believe, because
# every claim it could make is one `land` would have to re-derive anyway.
#
# `land` does NOT bound WHICH paths the resolution may touch. A path git merged
# cleanly is reported in the pushed-resolution comment, and joins the
# protected-path warning, but does not hold the push. The gate on that content is
# review: the remerge-diff report and the merge-delta reviewer, whose concern
# opens a blocking thread. So the push is fast and the merge is what waits.
#
# ── Why workflow_dispatch is the only trigger ─────────────────────────────────
#
# A conflict is created either by the PR branch moving or by the base branch
# moving underneath it. Neither arrives here as a PR event: GitHub dispatches no
# `pull_request` run for a PR it cannot merge, so a conflicted PR — the only kind
# this workflow acts on — never fires one. The merge-conflict labeler in
# pr-meta-privileged.yaml runs on pull_request_target (no merge ref needed) and
# dispatches this workflow with the PR it just labeled; land.sh re-dispatches it
# when a concurrent push made a resolution stale.
#
# The paid job also NEEDS a dispatch. claude-code-action rejects `push` events
# outright ("Unsupported event type: push"). And on push and schedule the
# `github.triggering_actor` the fan-out's actor gate reads is not the run's
# initiator at all — GitHub inherits it from whoever last acted on the branch —
# so the gate judges an unrelated account. A dispatch names its own initiator,
# and a bot dispatch names `github-actions`, which is the actor the gate admits.
#
# ONE PR PER DISPATCH, so `pr` is required. A whole-backlog scan is a rare
# operator action, not a mode this file carries:
#   gh pr list --label merge-conflict --json number --jq '.[].number' \
#     | xargs -I{} gh workflow run auto-resolve-conflicts.yaml -f pr={} -f catch-up=true
# ci-map: PR automation & review bots | Re-derives generated files and lockfiles deterministically plus an LLM pass over the remaining source conflicts, pushing the merge commit back to the PR head
name: Auto-resolve merge conflicts (reusable)

on:
  workflow_call:
    inputs:
      pr:
        description: "The PR number to resolve. Required: this workflow resolves one PR per call."
        required: true
        type: string
      catch-up:
        description: "Ignore the activity-age window and the one-attempt-per-head mark, so a PR whose head an earlier run already attempted is resolved anyway. Costs one paid resolve."
        required: false
        default: false
        type: boolean
      after-race:
        description: "Set by land.sh, not by a human: marks this run as the retry for a resolution a concurrent push made stale. Such a run dispatches no further retry."
        required: false
        default: false
        type: boolean
      resolver-repository:
        description: "The repository holding .github/resolver. Overriding it runs a fork's resolver against your PRs, with your secrets — change it only to a repository you control."
        required: false
        default: "AlexanderMattTurner/claude-automation-template"
        type: string
      resolver-ref:
        description: "Branch, tag or commit of resolver-repository to run. A caller that pins `uses: ...@<sha>` should pass the same sha here, so the workflow and the scripts it runs come from one commit."
        required: false
        default: "main"
        type: string
      protected-paths-regex:
        description: "ERE over repo-relative paths. A conflict inside one is still resolved, but the pushed-resolution comment flags it for human review. Empty takes the resolver default."
        required: false
        default: ""
        type: string
      resolver-mjs:
        description: "Repo-relative path to the caller's deterministic derived-file resolver. Two readers: the pre-pass retries with it when the merge left the PR's own copy conflicted, and the merge-delta report asks it which paths a required check re-derives, so it stops reading those. Both run the copy in the trusted base checkout. Empty declares no derived files, so no retry runs and every delta stays in the review."
        required: false
        default: ""
        type: string
      pre-pass-command:
        description: "Command that re-derives every conflicted generated file and lockfile in the caller's tree. Empty runs none, so every derived conflict reaches the model instead."
        required: false
        default: ""
        type: string
      model:
        description: "The model each per-file resolution shard runs."
        required: false
        default: "claude-opus-5"
        type: string
      bot-actors:
        description: "Space-separated actors the fan-out gate admits besides accounts the API reports as having write access."
        required: false
        default: "github-actions"
        type: string
      marked-regions:
        description: "The caller ships scripts/lib_marked_region.py, so the generated-region pre-pass runs. False skips it and every conflicted file reaches the model whole."
        required: false
        default: false
        type: boolean
      log-redactor:
        description: "Repo-relative path in resolver-repository to a redactor for the fan-out logs. EMPTY PUBLISHES NO LOGS: an agent transcript carries whatever the merge exposed it to, so the absence of a redactor fails closed rather than uploading it raw."
        required: false
        default: ""
        type: string
    # Named, never `secrets: inherit`. This workflow is written to be called from
    # another repository, so the list IS the contract a consumer configures
    # against, and inheriting would hand a resolver run every unrelated secret the
    # calling repository holds. Each is optional: an unset rung is dropped by
    # run-ladder.py, and an unset org PAT narrows what `land` can push rather
    # than failing the call. tests/test_auto_resolve_reusable_secrets.py asserts
    # this list covers every `secrets.` the file reads, because an undeclared one
    # arrives EMPTY and reads as a dead credential rather than as a mistake.
    secrets:
      FAR_ANTHROPIC_API_KEY:
        description: "Rung 1: the org's metered Anthropic key, spent before any subscription token."
        required: false
      CLAUDE_CODE_OAUTH_TOKEN_FALLBACK:
        description: "Rung 2: first subscription token."
        required: false
      CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_2:
        description: "Rung 3: second subscription token."
        required: false
      CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_3:
        description: "Rung 4: third subscription token."
        required: false
      CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_4:
        description: "Rung 5: fourth subscription token."
        required: false
      CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_5:
        description: "Rung 6: fifth subscription token."
        required: false
      CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_6:
        description: "Rung 7: sixth subscription token."
        required: false
      CLAUDE_CODE_OAUTH_TOKEN:
        description: "Rung 8: the last subscription token the ladder reaches."
        required: false
      AUTOFIX_TOKEN_ORG:
        description: "Push credential for the resolved merge commit. Absent, `land` falls back to the job's GITHUB_TOKEN."
        required: false
      TEMPLATE_SYNC_TOKEN_ORG:
        description: "Workflow-scoped PAT, the only credential GitHub lets push a merge that changes .github/workflows/. Absent, such a merge is refused and the PR is labeled auto-resolve-blocked."
        required: false

permissions: {}

jobs:
  resolve:
    # untrusted-exec-ok: this workflow has only a workflow_dispatch trigger, which a fork PR cannot fire — so a fork's head is never checked out by a run holding secrets; every credential the lint names exists only on a SAME-REPO run, whose head branch already required push access to create. This job narrows further: discover.py drops every cross-repository PR (pinned by tests/test_auto_resolve_discover.py), so the head_ref it checks out is always a same-repo branch. The lint cannot see either fact — the trigger set and the fork filter both live outside this job's block.
    name: Auto-resolve merge conflicts
    # untrusted-exec-ok: the two local composite actions below resolve from the
    # PR-head checkout, but `discover` emits only SAME-REPO heads (it drops
    # forks), so rewriting them requires push access to this repo — the anonymous
    # pwn-request attacker this class exists to stop cannot reach this job. The
    # resolver logic itself is staged from the default branch, and every write
    # credential lives on `land`.
    runs-on: ubuntu-latest
    # ONE budget, derived here and nowhere else. Each line is a stage of this job
    # and the knob that bounds it; their sum is the timeout below, and 55 is the
    # cap this derivation may not exceed.
    #   fan-out, whatever the conflict's size and however many rungs it takes  20
    #     (FANOUT_BUDGET_SECONDS: 1200, which every rung shares through the
    #      deadline auto-resolve/run-ladder.py stamps once)
    #   pre-push self-review, worst case  12  (2 reviews + 1 fix at a 240s cap)
    #   bundle's hook-repair pass  10  (SHARD_TIMEOUT_SECONDS for the whole ladder)
    #   fan-out log staging  5  (the stage_logs step's own timeout-minutes below)
    #   discover, the checkouts and the toolchain installs  2
    # A run killed here pushes nothing, so the budget has to cover those stages
    # rather than truncate one.
    timeout-minutes: 49
    # Per-PR, and never cancelling: two resolves of one PR are redundant, but
    # cancelling one is pure loss — on a base branch that advances faster than a
    # resolve takes, no resolve would ever reach its push. (Observed 2026-08-04:
    # PR #3429's resolve was cancelled at 44s, restarted, and cancelled again at
    # 3m54s; eleven PRs stayed conflicted through four scans.) A queued duplicate
    # is cheap rather than paid: this job's first step is discover, which re-reads
    # the attempt mark the running job's `mark` step wrote, so the duplicate
    # refuses before any model call. GitHub holds at most one pending run per
    # group, which bounds the duplicate at one.
    concurrency:
      group: auto-resolve-pr-${{ inputs.pr }}
      cancel-in-progress: false
    permissions:
      # NO write to contents, and that is the point — see the trust model above.
      # This job runs the PR's own composites, the PR's own `pnpm` scripts and the
      # model, so these scopes are the ceiling on what any of them reach.
      # `pull-requests: write` is the stated residual: the pre-LLM handoff, the
      # failure diagnoses and discover's terminal-state notice have to land on the
      # PR they are about, and it confers no ability to write a branch.
      contents: read
      pull-requests: write # comment on the PR (handoff / failure diagnosis)
      # `gh label create`/`--add-label` for auto-resolve-blocked rides the issues
      # API. This job applies it in one case only: leftover conflict markers whose
      # cause was a DENIED EDIT TOOL — a broken grant that every future run would
      # repeat at full LLM cost, so the treadmill has to stop where the evidence
      # is. Conferring no branch write, it stays within this job's read-only-to-
      # code ceiling.
      issues: write
      statuses: write # mark the head commit as attempted, so no later scan repeats it
      actions: read # the reuse step lists this workflow's prior runs and downloads its own artifact
    outputs:
      # `land` runs in its own job and cannot read this one's $GITHUB_ENV, so the
      # fields it needs cross as outputs.
      selected: ${{ steps.selected.outputs.selected }}
      head_ref: ${{ steps.selected.outputs.head_ref }}
      base_ref: ${{ steps.selected.outputs.base_ref }}
      head_sha: ${{ steps.selected.outputs.head_sha }}
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GH_REPO: ${{ github.repository }}
      # Stands in for claude-code-action's `allowed_bots: github-actions`: the
      # labeler and land.sh both dispatch with github.token, so their runs'
      # initiating actor is the github-actions bot. The fan-out's gate admits
      # exactly that actor plus actors the API affirmatively reports as having
      # write access, and denies everything else — including the silent-failure
      # cases (no token, a 5xx, a 404 non-collaborator). The dispatch-only
      # trigger is what makes the value meaningful: a dispatch names its own
      # initiator, while push and schedule inherit an unrelated one.
      TRIGGERING_ACTOR: ${{ github.triggering_actor }}
      # How many per-file resolutions may run at once. Bounded on purpose: the
      # shards share one LLM credential and the runner's CPU, so an unbounded
      # fan-out trades the serial-latency problem for a contention one.
      MAX_PARALLEL: "4"
      # Caller-supplied knobs. Each one names a capability the CALLING repository
      # has and this workflow cannot assume: which paths it treats as protected,
      # how it re-derives a generated file, whether it ships marked regions. An
      # empty value is "the caller does not have this", never "use a guess".
      AUTO_RESOLVE_PROTECTED_RE: ${{ inputs.protected-paths-regex }}
      # `resolver-mjs` is absent here on purpose: it arrives repo-relative and the
      # two steps that run it need the trusted base's copy, so the `base` step
      # absolutizes it and they read `steps.base.outputs.resolver_mjs`.
      AUTO_RESOLVE_PRE_PASS: ${{ inputs.pre-pass-command }}
      AUTO_RESOLVE_MARKED_REGIONS: ${{ inputs.marked-regions }}
      AUTO_RESOLVE_MODEL: ${{ inputs.model }}
      AUTO_RESOLVE_BOT_ACTORS: ${{ inputs.bot-actors }}
      # Wall-clock bound on ONE per-file resolution. It has to cover the WRITE,
      # not just the reasoning: a shard reads both sides, then rewrites the whole
      # merged file through Edit calls, and a kill lands on whichever of those it
      # is in. At 420s PR #3826 timed out three rungs on one 947-line file, each
      # having understood the conflicts in 92s and then spent every remaining
      # second writing. 600s is fanout.py's own SHARD_TIMEOUT_DEFAULT, which this
      # no longer contradicts.
      SHARD_TIMEOUT_SECONDS: "600"
      # Wall-clock bound on the fan-out as a WHOLE, which is what keeps the ladder
      # inside the 20 minutes the job budget above reserves for it. The per-shard
      # bound cannot: shards run MAX_PARALLEL at a time, so 12 conflicted files
      # are three waves and 1800s. Past `timeout-minutes` GitHub cancels the job
      # rather than failing the step, so no later step runs — the bundle is never
      # built and every shard's finished resolution is discarded with the runner.
      FANOUT_BUDGET_SECONDS: "1200"
    steps:
      # Three trees, and which one a step reads is the whole security model:
      #   ${RUNNER_TEMP}/resolver  the code doing the merging, cloned from a
      #                            repository the pull request cannot write
      #   ${RUNNER_TEMP}/base      the calling repository at the dispatched ref,
      #                            carrying the caller's own capabilities — its
      #                            redactor, its pinned engine, its prompts
      #   ${GITHUB_WORKSPACE}      the pull request head, left MID-MERGE from the
      #                            prepare step onward, untrusted throughout
      # Both trusted trees sit OUTSIDE the workspace, so the PR-head checkout
      # below cannot replace them and no later step has to re-stage them.
      - name: Check out the resolver from its own repository
        # THE REASON THIS IS A REUSABLE WORKFLOW AND NOT A COMPOSITE ACTION.
        # A local `uses: ./...` resolves against the WORKSPACE, which in this job is
        # the pull request's own copy left MID-MERGE. The runner would read a
        # manifest the pull request wrote, and a manifest that is itself conflicted
        # is not YAML, so every credential rung died before the resolver started.
        # This job used to answer that by staging a second worktree off the default
        # branch by hand, inline, because the bootstrap could not itself live in a
        # script it had not yet staged.
        #
        # A reusable workflow is fetched by the runner from ITS OWN repository, so
        # the resolver arrives by clone from a repository the pull request cannot
        # write. The tree being merged and the code doing the merging are now
        # different repositories, which is what the staging worktree was
        # approximating.
        id: resolver
        env:
          RESOLVER_REPO: ${{ inputs.resolver-repository }}
          RESOLVER_REF: ${{ inputs.resolver-ref }}
        run: |
          dest="${RUNNER_TEMP}/resolver"
          git clone --no-tags "https://github.com/${RESOLVER_REPO}.git" "${dest}"
          # A ref may be a branch, a tag or a commit SHA. `checkout --detach` takes
          # all three; `clone --branch` refuses a SHA, which is the form a caller
          # pinning its `uses:` line has.
          git -C "${dest}" checkout --detach "${RESOLVER_REF}"
          echo "dir=${dest}/.github/resolver" >>"$GITHUB_OUTPUT"

      - name: Check out the calling repository's trusted base
        # A CLONE, not `actions/checkout`: that action writes only inside
        # GITHUB_WORKSPACE, and this tree has to outlive the PR-head checkout that
        # replaces the workspace a few steps below.
        #
        # The dispatched ref, never the pull request's own content. Discover's
        # answer drives every paid step here, and three later steps read this tree
        # for something the pull request must not supply: the self-review's
        # prompts, the redaction engine's pinned project, and the redactor itself.
        id: base
        env:
          GH_TOKEN: ${{ github.token }}
          CALLER_REPO: ${{ github.repository }}
          CALLER_SHA: ${{ github.sha }}
          RESOLVER_MJS_REL: ${{ inputs.resolver-mjs }}
        run: |
          dest="${RUNNER_TEMP}/base"
          git clone --no-tags \
            "https://x-access-token:${GH_TOKEN}@github.com/${CALLER_REPO}.git" "${dest}"
          git -C "${dest}" checkout --detach "${CALLER_SHA}"
          # Drop the credential the clone left in the remote URL. Nothing below
          # pushes from this tree, and a paid model runs later in this job.
          git -C "${dest}" remote set-url origin "https://github.com/${CALLER_REPO}.git"
          echo "dir=${dest}" >>"$GITHUB_OUTPUT"
          # The caller names its derived-file resolver repo-relative, because that
          # is the only form it knows. Every step that runs the resolver has the
          # untrusted PR head as its cwd, so absolutize the path against THIS tree
          # once, here, and let those steps read it back. The relative form is
          # never exported: it would name the copy the pull request wrote, and one
          # of its two readers decides which deltas the reviewer stops reading.
          # Empty stays empty — the caller ships no derived files.
          echo "resolver_mjs=${RESOLVER_MJS_REL:+${dest}/${RESOLVER_MJS_REL}}" >>"$GITHUB_OUTPUT"

      - name: Discover whether this PR is still worth resolving
        id: discover
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          # One PR per dispatch, so the scan costs one PR instead of the backlog.
          PR_NUMBER: ${{ inputs.pr }}
          # A human-dispatched catch-up drops both spend filters at once: the
          # filters exist to stop an automatic trigger paying for the same PR
          # repeatedly, and neither concern applies to a run someone asked for.
          AUTO_RESOLVE_MAX_COMMIT_AGE_HOURS: ${{ inputs.catch-up && '0' || '' }}
          AUTO_RESOLVE_IGNORE_ATTEMPT_MARK: ${{ inputs.catch-up }}
          # A repository variable, so an operator flips the mode without a PR.
          # Actions surfaces `vars.` only through an expression, so a knob the
          # script reads from the environment has to be named here or it is
          # unreachable in production and the script always takes its default.
          AUTO_RESOLVE_CHAINED_CHILDREN: ${{ vars.AUTO_RESOLVE_CHAINED_CHILDREN }}
          # Which repository and ref hold the resolver, so the handoff-mark probe
          # asks THAT repository when the resolver's own code last changed. The
          # mark records a verdict about this program; reading the caller's tree
          # for it now finds nothing, and a mark nothing retires strands the PR.
          AUTO_RESOLVE_RESOLVER_REPO: ${{ inputs.resolver-repository }}
          AUTO_RESOLVE_RESOLVER_REF: ${{ inputs.resolver-ref }}
          # Every step below reaches the resolver through this name rather than
          # expanding the step output inside its `run:` body. A step's own `env:`
          # outranks the job environment, so a write to $GITHUB_ENV from the
          # untrusted workspace cannot repoint which tree the next step executes.
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
        # No bootstrap guard: these scripts come from the resolver repository at
        # the ref the caller pinned, so an absent one is a bad `resolver-ref`, not
        # a default branch that has not caught up yet. Failing loud names it.
        working-directory: ${{ steps.base.outputs.dir }}
        run: python3 "${RESOLVER_DIR}/auto-resolve/discover.py"

      - name: Read the PR discover selected
        id: selected
        env:
          PRS: ${{ steps.discover.outputs.prs }}
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
        working-directory: ${{ steps.base.outputs.dir }}
        run: python3 "${RESOLVER_DIR}/auto-resolve/selected-pr.py"

      - name: Reuse a prior run's still-valid resolution
        id: reuse
        # A resolution held back at land time — a merge-queue hold, a land-job
        # crash, a cancel after the upload — is still exactly the merge this run
        # would buy while the PR head has not moved. reuse-bundle.py fills the
        # bundle directory from the newest prior artifact recording this head;
        # on a hit every paid step below is skipped, the upload step
        # re-publishes the bundle as this run's artifact, and `land` verifies
        # and pushes it through its unchanged checks. A hit also sets no
        # attempt mark: it spends nothing the mark exists to protect, and
        # land.sh marks the head it pushes. The script pins that artifact's
        # producer to this workflow file on GITHUB_REF_NAME, which Actions
        # already sets for every step, so it takes no env entry below.
        # hit=false is a normal resolve.
        if: steps.selected.outputs.selected == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          PR: ${{ inputs.pr }}
          HEAD_SHA: ${{ steps.selected.outputs.head_sha }}
          BUNDLE_DIR: ${{ runner.temp }}/auto-resolve-bundle
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
        working-directory: ${{ steps.base.outputs.dir }}
        run: python3 "${RESOLVER_DIR}/auto-resolve/reuse-bundle.py"

      - name: Checkout PR head
        if: steps.selected.outputs.selected == 'true' && steps.reuse.outputs.hit != 'true'
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # A same-repo branch name (discover drops forks), so this is a trusted
          # ref; scripts authenticate git out-of-band, so no push credential is
          # persisted while PR code runs.
          ref: ${{ steps.selected.outputs.head_ref }}
          fetch-depth: 0
          persist-credentials: false

      - name: Set up Node + install deps
        if: steps.selected.outputs.selected == 'true' && steps.reuse.outputs.hit != 'true'
        uses: ./.github/actions/setup-base-env

      - name: Set up uv
        if: steps.selected.outputs.selected == 'true' && steps.reuse.outputs.hit != 'true'
        # The pre-pass's lockfile rules need the `uv` binary (for `uv lock`) to
        # deterministically re-derive uv.lock from a merged pyproject.toml; only
        # the binary — no `uv sync`, so the resolver is not coupled to the PR
        # head's venv being installable. A direct pinned step, NOT a local
        # composite — neither setup-base-env's `setup-uv` input nor
        # `uv-setup-retry`, which every other job here now uses. Both are read
        # from the PR-HEAD checkout, and a head branch that predates them ignores
        # the input or cannot resolve the path, so uv never installs and every
        # rung of the resolver dies at the hook-tools step before any resolution.
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Mark this head commit as attempted
        id: mark
        # As early as the trusted scripts allow, and well before the merge and any
        # model call: the mark exists to stop a SECOND run against this tree, and
        # the runs worth stopping are exactly the ones that end badly (a crash, a
        # timeout, a cancellation) and so never reach a step at the end. Runs from
        # the base-staged copy for the same reason every other resolver script
        # does — this one posts a commit status with the write token, so letting
        # the PR head supply its body would hand a pull request that token.
        if: steps.selected.outputs.selected == 'true' && steps.reuse.outputs.hit != 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          # The catch-up input exists to re-run heads a mark is holding, so it has
          # to reach the claim check too — otherwise every catch-up run stands down
          # on the very marks it was dispatched to ignore.
          AUTO_RESOLVE_IGNORE_ATTEMPT_MARK: ${{ inputs.catch-up }}
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
        run: bash "${RESOLVER_DIR}/auto-resolve/mark-attempt.sh"

      # Every step from here on reads `steps.mark.outcome == 'success'` as well as
      # the claim: a run discover selected nothing for skips `mark`, whose
      # `already_claimed` output is then EMPTY — and empty is not 'true', so the
      # claim test alone would let every paid step below run with no PR selected.
      - name: Say on the PR that this run took the conflict on
        if: steps.mark.outcome == 'success' && steps.mark.outputs.already_claimed != 'true'
        # Posted BEFORE the merge and any model call, for the same reason the mark
        # above is: a run that dies mid-resolution never reaches a step at the end,
        # and that is exactly the run whose silence is unreadable on the PR. The
        # comment is rewritten in place by whichever step ends this attempt — the
        # handoff, the land verdict, or the always() step at the end of each job —
        # so the PR carries ONE auto-resolve comment that states the current answer.
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          STATE: working
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
        run: bash "${RESOLVER_DIR}/auto-resolve/status-comment.sh"

      - name: Install mergiraf (pinned)
        if: steps.mark.outcome == 'success' && steps.mark.outputs.already_claimed != 'true'
        # Prepare's structural pre-pass needs the binary and dies loud without
        # it. Installed from the resolver's own tree for the same reason the
        # resolver runs from it: the PR head must not choose what binary the
        # resolver trusts.
        env:
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
        run: bash "${RESOLVER_DIR}/install-mergiraf.sh"

      - name: Cache pre-commit hook environments
        if: steps.mark.outcome == 'success' && steps.mark.outputs.already_claimed != 'true'
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/pre-commit
          key: ${{ runner.os }}-precommit-${{ hashFiles('.pre-commit-config.yaml') }}
          restore-keys: |
            ${{ runner.os }}-precommit-

      - name: Install pre-commit
        if: steps.mark.outcome == 'success' && steps.mark.outputs.already_claimed != 'true'
        # bundle runs the repo's hooks over the resolved paths before it commits,
        # and fails loud when the binary is absent rather than pushing content no
        # linter ever saw — so this install is load-bearing, not a convenience.
        # The resolver's own copy resolves the pin, for the same reason every
        # other resolver script runs from it: sourcing the PR head's
        # tool-versions.sh would let the pull request choose what this
        # write-token job installs.
        env:
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
        run: bash "${RESOLVER_DIR}/pip-install-ci-tools.sh" pre-commit

      - name: Install the pinned hook toolchain (binaries + Python packages)
        if: steps.mark.outcome == 'success' && steps.mark.outputs.already_claimed != 'true'
        # Ordered AFTER the staged worktree above because that is where the version pins
        # are read from — .github/tool-versions.sh for the binaries, pyproject.toml's dev
        # extra for the Python packages. Sourcing the PR head's copies would let
        # the pull request choose what this write-token job installs and runs. It
        # installs only the hook toolchain, so it never runs `pnpm install`/`uv sync`
        # over the PR's own manifests.
        env:
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
        run: bash "${RESOLVER_DIR}/auto-resolve/install-hook-tools.sh"

      - name: Merge base, deterministic pre-pass, and protected-path flag
        id: prepare
        if: steps.mark.outcome == 'success' && steps.mark.outputs.already_claimed != 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
          # The trusted base's copy of the caller's derived-file resolver. The
          # pre-pass runs the PR's own copy first; this is the retry that still
          # re-derives when the merge left that copy conflicted.
          AUTO_RESOLVE_RESOLVER_MJS: ${{ steps.base.outputs.resolver_mjs }}
        run: bash "${RESOLVER_DIR}/auto-resolve/prepare.sh"

      - name: Release this head's attempt mark (this run resolved nothing)
        # The mark buys ONE resolve per head; a no-op run bought nothing with it,
        # so it hands it back instead of suppressing every scan of this PR for a
        # full TTL. The commonest way to reach here is the resolver's own land
        # push re-triggering this workflow onto the head it just resolved.
        #
        # NOT gated on the unresolvable handoff below, which is a verdict rather
        # than a no-op: a binary conflict still needs a human, and re-running
        # against the same tree reaches the same wall. prepare sets this output on
        # the no-op exits only, so the two paths cannot both fire.
        if: steps.prepare.outputs.no_op_head != ''
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          HEAD_SHA: ${{ steps.prepare.outputs.no_op_head }}
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
        run: bash "${RESOLVER_DIR}/auto-resolve/release-attempt.sh"

      - name: Hand off unmergeable (binary) conflicts to a human
        id: handoff
        # A binary conflict (or a `-merge` file owned by no rule) has no textual
        # resolution an LLM could produce and no deterministic re-derivation. A
        # `-merge` lockfile IS owned by a rule, so it is re-derived by the
        # pre-pass and never reaches here. Gated on needs_llm/needs_commit both
        # false: prepare.sh keeps an unresolvable path's own content and lets
        # the rest of the pipeline run when other work exists, so this step
        # fires only when nothing else in the PR needed resolving either.
        if: |
          steps.prepare.outputs.unresolvable != '' &&
          steps.prepare.outputs.needs_llm != 'true' &&
          steps.prepare.outputs.needs_commit != 'true'
        env:
          UNRESOLVABLE: ${{ steps.prepare.outputs.unresolvable }}
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
        run: bash "${RESOLVER_DIR}/auto-resolve/handoff.sh"

      - name: Resolve the remaining source conflicts with Claude
        id: ladder
        # ONE step for the WHOLE credential ladder. auto-resolve/_ladder.py states
        # the retry policy once and auto-resolve/run-ladder.py walks it, spending
        # each rung's credential on the same base-staged attempt script
        # (claude-conflict-resolve.sh → auto-resolve/fanout.py) so the resolver's
        # security-relevant config (the actor gate, --permission-mode acceptEdits,
        # the bounded tool set, the file-scope prompt) has a single SSOT.
        #
        # A base-staged script, never a local `uses: ./…` composite: the runner
        # reads a local action's manifest out of the WORKSPACE at step time, and
        # this job's workspace is the untrusted PR head left MID-MERGE — when the
        # manifest itself is one of the conflicted files, its conflict markers are
        # not YAML and every rung dies before the resolver starts. The same reason
        # rules out the claude-code-with-fallback composite the reviewers use, on
        # top of that composite having no same-token zero-cost free retry.
        #
        # The loop holds EVERY rung's secret because choosing between them is its
        # job; each attempt's CHILD process gets exactly one, which is what keeps
        # a paid attempt attributable to one token.
        if: steps.prepare.outputs.needs_llm == 'true'
        env:
          CONFLICT_LIST: ${{ steps.prepare.outputs.conflict_list }}
          MODIFY_DELETE_PATHS: ${{ steps.prepare.outputs.modify_delete }}
          SIDECAR_PATHS: ${{ steps.prepare.outputs.sidecar }}
          # Rung 1 is the metered FAR_ANTHROPIC_API_KEY: a resolve run spends
          # the org's own funded key first and reaches a subscription token only
          # once it errors, with CLAUDE_CODE_OAUTH_TOKEN — the maintainer's
          # personal account — last. An empty rung is not a stop: run-ladder.py
          # drops it and reaches the configured rungs behind it.
          # BEGIN GENERATED: credential ladder rung-tokens (.github/scripts/gen_credential_ladder.py) — do not edit by hand
          RUNG_1_TOKEN: ${{ secrets.FAR_ANTHROPIC_API_KEY }}
          RUNG_2_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK }}
          RUNG_3_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_2 }}
          RUNG_4_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_3 }}
          RUNG_5_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_4 }}
          RUNG_6_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_5 }}
          RUNG_7_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_6 }}
          RUNG_8_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          # END GENERATED: credential ladder rung-tokens
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
        run: /usr/bin/python3 "${RESOLVER_DIR}/auto-resolve/run-ladder.py"

      - name: Fail loud if the Claude resolution errored
        id: assert_llm
        # claude-code-action exposes no success/failure output and does NOT fail
        # the step on an internal error (is_error) — so a zero-edit auth/model
        # failure slips through as green, leaving the markers for finalize to
        # misreport as an unresolvable conflict handed to a human. Read the
        # execution log and stop here instead, so a broken CLAUDE_CODE_OAUTH_TOKEN
        # or an inaccessible --model surfaces as itself. A genuine "conflict too
        # hard" run has is_error false (Claude left the markers on purpose per the
        # prompt) and passes this gate, so finalize still posts the human handoff.
        #
        # The permission denials are exported (not failed on): with
        # --permission-mode acceptEdits the resolver's edits are auto-accepted, so
        # a denial means it reached for a tool it wasn't granted (never fatal on
        # its own — the edit may still have landed). Both the count AND the denied
        # tool NAMES are exported, because only the names can tell the two causes
        # of leftover markers apart downstream: a denied Edit/Write really did
        # close the write path, while a denied Bash/TodoWrite left the resolver
        # free to edit and says nothing about why the markers survived.
        if: steps.prepare.outputs.needs_llm == 'true'
        env:
          # The newest rung that produced a log. A rung that died before the
          # fan-out wrote one publishes nothing, so the loop republishes the
          # newest that did — this asserts on the attempt that has evidence.
          EXECUTION_FILE: ${{ steps.ladder.outputs.execution_file }}
          CONTEXT: Claude conflict resolution
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
        run: /usr/bin/python3 "${RESOLVER_DIR}/checks/claude-execution.py"

      - name: Release this head's attempt mark (every credential billed nothing)
        # The mark is priced in MONEY: it stops a base push re-running a PAID
        # resolve against a tree that just produced one. A ladder whose every rung
        # reported zero_cost never reached the model — a weekly cap, a revoked
        # token, a CLI that failed to install — so this run bought nothing and
        # hands the attempt back, exactly as the no-op path above does.
        #
        # `release_attempt` reads EVERY rung the loop ran, not just the last: a
        # rung that spent real money and failed DID buy something, and a re-run
        # spends again on the same wall.
        #
        # assert_llm must have FAILED: a resolution that reached the model and then
        # lost on the tree keeps its mark, because a re-run reproduces that verdict.
        # No bootstrap guard is needed — `head_sha` is only set by a staged
        # mark-attempt.sh from this commit or later, which carries release-attempt.sh.
        if: >-
          always()
          && steps.assert_llm.outcome == 'failure'
          && steps.mark.outputs.head_sha != ''
          && steps.ladder.outputs.release_attempt == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          HEAD_SHA: ${{ steps.mark.outputs.head_sha }}
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
        run: bash "${RESOLVER_DIR}/auto-resolve/release-attempt.sh"

      - name: Verify, self-review, and bundle the merge for the land job
        id: bundle
        # `!cancelled()`, so a ladder that ENDED on a dead credential still bundles
        # what the rungs before it resolved. assert_llm says the model errored; it
        # never says the tree is bad, and this step is what decides that — it
        # refuses a tree still carrying markers and it runs the hooks. Gating it on
        # assert_llm instead discarded a paid resolution and posted no handoff: on
        # PR #4093 rungs 3-5 billed $5.08 and resolved 2 of 3 files, rung 6 answered
        # a weekly-limit 429, and the run ended with the work thrown away and the
        # unresolvable file never named. Not `always()`: a cancelled job must not push.
        if: "!cancelled() && steps.prepare.outputs.needs_commit == 'true'"
        env:
          # The pre-bundle self-review reads the merge commit the way the
          # post-push watchdog will and lets a model correct what it flags, so a
          # flagged resolution is never handed to `land`. Prompts and the CLI
          # installer are read from the TRUSTED base worktree, never the PR head
          # it is reviewing.
          BASE_WORKTREE: ${{ steps.base.outputs.dir }}
          # Which paths a required check re-derives, so the delta report stops
          # reading them. From the base worktree for the same reason: the PR's own
          # copy could claim its evil merge is generated and skip the review.
          AUTO_RESOLVE_RESOLVER_MJS: ${{ steps.base.outputs.resolver_mjs }}
          # The same ordered credential set the resolve ladder above walks.
          # self_review.py advances a rung only when one produced NO verdict (the
          # shape of a dead token), never on a verdict — so this cannot turn a
          # flagged resolution into a clean one, and an every-rung-dead run is
          # still a refusal rather than a bundle of an unreviewed merge. Giving
          # this step only the primary is what took the resolver down for every
          # PR once that one token expired: the fan-out routed around it and the
          # reviewer could not.
          # BEGIN GENERATED: credential ladder bundle-secrets (.github/scripts/gen_credential_ladder.py) — do not edit by hand
          FAR_ANTHROPIC_API_KEY: ${{ secrets.FAR_ANTHROPIC_API_KEY }}
          CLAUDE_CODE_OAUTH_TOKEN_FALLBACK: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK }}
          CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_2: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_2 }}
          CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_3 }}
          CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_4: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_4 }}
          CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_5: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_5 }}
          CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_6: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_6 }}
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          # END GENERATED: credential ladder bundle-secrets
          # BEGIN GENERATED: credential ladder preferred-token (.github/scripts/gen_credential_ladder.py) — do not edit by hand
          # The resolve ladder already proved one credential can reach the model. Review
          # with it first instead of replaying each exhausted rung. The loop publishes the
          # WINNING rung's own secret NAME — a winner whose own secret is unset (the rung-2
          # same-credential free retry) names the rung before it, per
          # auto-resolve/_ladder.py's `preferred_token_env`. These arms turn that name into
          # its value, which no expression can index by name.
          RESOLVER_PREFERRED_TOKEN: >-
            ${{ steps.ladder.outputs.preferred_token_env == 'FAR_ANTHROPIC_API_KEY' && secrets.FAR_ANTHROPIC_API_KEY
            || steps.ladder.outputs.preferred_token_env == 'CLAUDE_CODE_OAUTH_TOKEN_FALLBACK' && secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK
            || steps.ladder.outputs.preferred_token_env == 'CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_2' && secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_2
            || steps.ladder.outputs.preferred_token_env == 'CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_3' && secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_3
            || steps.ladder.outputs.preferred_token_env == 'CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_4' && secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_4
            || steps.ladder.outputs.preferred_token_env == 'CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_5' && secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_5
            || steps.ladder.outputs.preferred_token_env == 'CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_6' && secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_6
            || steps.ladder.outputs.preferred_token_env == 'CLAUDE_CODE_OAUTH_TOKEN' && secrets.CLAUDE_CODE_OAUTH_TOKEN
            || '' }}
          # END GENERATED: credential ladder preferred-token
          # The winning rung's user-facing label; bundle.py records it for the PR comment.
          RESOLVED_RUNG_LABEL: ${{ steps.ladder.outputs.rung_label }}
          CONFLICT_LIST: ${{ steps.prepare.outputs.conflict_list }}
          DEFERRED_REGEN: ${{ steps.prepare.outputs.deferred_regen }}
          # The marker-free conflicts: one side deleted the path, the other
          # changed it. Bundle stages each from the resolver's recorded verdict
          # rather than from the working tree — which holds the surviving side's
          # content either way.
          MODIFY_DELETE_PATHS: ${{ steps.prepare.outputs.modify_delete }}
          # Whichever rung of the credential ladder actually ran wrote the
          # verdicts; same precedence as the execution log above.
          MODIFY_DELETE_VERDICTS: ${{ steps.ladder.outputs.verdict_file }}
          # The conflicts the resolver may read but not write in place: its
          # shard wrote each merged file to a scratch path outside the repo,
          # and bundle installs them before staging. Same rung precedence.
          SIDECAR_PATHS: ${{ steps.prepare.outputs.sidecar }}
          SIDECAR_RESOLUTIONS: ${{ steps.ladder.outputs.resolution_file }}
          # Where the fan-out wrote its logs, same rung precedence. bundle's
          # hook-repair pass puts its own logs under repair-<rung>/ here, so the
          # log-staging step below publishes them with the shard logs.
          FANOUT_DIR: ${{ steps.ladder.outputs.fanout_dir }}
          # Empty when the LLM step was skipped (deterministic-only resolution);
          # bundle defaults it to 0. Non-zero + leftover markers ⇒ the resolver
          # may have been blocked, so the handoff comment names the real cause.
          LLM_PERMISSION_DENIALS: ${{ steps.assert_llm.outputs.permission_denials }}
          # WHICH tools were denied, as a JSON array (or `null` when the log could
          # only count them). The count alone never established that edits were
          # blocked: under `--permission-mode acceptEdits` a denial is normally a
          # tool outside the granted set (a Bash probe, TodoWrite), which leaves
          # editing fully available — so bundle blames permissions for leftover
          # markers only when an EDIT tool is in here.
          LLM_PERMISSION_DENIED_TOOLS: ${{ steps.assert_llm.outputs.permission_denied_tools }}
          # The same denials attributed to the shard that hit them, as a
          # `{file: [tool, …]}` object. The array above is a UNION over a fan-out
          # of one shard per conflicted file, so it cannot say whether the denied
          # shard is the one that left markers: bundle joins this against the
          # files still carrying markers before it blames the grants and labels
          # the PR out of auto-resolve.
          LLM_PERMISSION_DENIALS_BY_FILE: ${{ steps.assert_llm.outputs.permission_denials_by_file }}
          BUNDLE_DIR: ${{ runner.temp }}/auto-resolve-bundle
          # No SKIP here: bundle.py derives the hooks this job must not run from
          # .pre-commit-config.yaml itself (hooks_needing_the_project_env), so
          # the set cannot drift from the entries it names.
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
        run: python3 "${RESOLVER_DIR}/auto-resolve/bundle.py"

      # !cancelled(), not always(): a resolve that failed has already commented
      # its own diagnosis and is red, and `land` treats a directory with no
      # `merge.bundle` as "nothing to push" rather than as a second failure to
      # report — but a leftover-markers refusal writes a `salvage.patch` for the
      # files it DID resolve into this same directory, and that file must still
      # reach the artifact a failed run produces no other upload for. A
      # cancelled job must not push, so cancellation stays excluded.
      - name: Upload the resolved merge for the land job
        if: "!cancelled()"
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: auto-resolve-merge-${{ inputs.pr }}
          path: ${{ runner.temp }}/auto-resolve-bundle
          if-no-files-found: ignore
          # The discard comments and the salvage note tell a human this
          # artifact still holds the thrown-away resolution; a human reads
          # those comments days later, so the window matches the other
          # human-facing artifacts below rather than the land job's minutes.
          retention-days: 14

      - name: Install the redaction engine from the trusted base
        id: engine
        # The stager below masks the fan-out through agent_sanitizer and publishes
        # nothing — loudly — without it, so this install is what makes the artifact
        # exist. It cannot arrive the way every other publisher's does (setup-base-env
        # with `setup-python: "true"`, i.e. `uv sync` over the WORKSPACE): from the
        # prepare step onward the workspace is the untrusted PR head left MID-MERGE,
        # so that sync would install whatever packages the pull request's own lockfile
        # names, inside the job holding the resolution and the billing tokens. The
        # base worktree carries the same pinned engine on a reviewed ref, into a venv
        # the PR never wrote. Its interpreter reaches the stager as PYTHON rather than
        # on PATH, so no other step's `python3` changes meaning.
        if: always() && steps.prepare.outputs.needs_llm == 'true' && inputs.log-redactor != ''
        env:
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
          BASE_DIR: ${{ steps.base.outputs.dir }}
        run: |
          bash "${RESOLVER_DIR}/uv-sync-retry.sh" --project "${BASE_DIR}"
          echo "python=${BASE_DIR}/.venv/bin/python3" >>"$GITHUB_OUTPUT"

      - name: Say on the PR that this run produced no resolution
        # The half of the announcement that makes it trustworthy: without it a run
        # that crashed, was cancelled or hit its timeout leaves "working on it"
        # standing forever, which reads as a resolver still trying. always(), so it
        # survives every one of those endings.
        #
        # Skipped when a bundle exists — `land` pushes that resolution and states
        # its own verdict — and the script rewrites nothing that already carries a
        # verdict, so the handoff's refusal survives this step. A run that found
        # the base already merges cleanly says THAT instead of "gave up", which is
        # a different answer to the reader's question.
        #
        # Ordered BEFORE log staging, which is unbounded in the job's budget: one
        # run spent 19 minutes redacting 1192 files, hit the job cap, and was
        # cancelled 19 seconds into this step, so the PR never heard the verdict at
        # all. The announcement is the only output a human waits on, so it goes
        # first and the diagnostics follow.
        #
        # Skipped when this run stood down on another run's claim, or when discover
        # selected nothing: it spent nothing, so it has no ending to announce, and
        # the run that owns the head is still resolving. The comment protocol
        # refuses the rewrite too (the in-flight marker names the run that wrote
        # it), so this gate is what saves the API call rather than the comment.
        if: >-
          always()
          && steps.mark.outcome == 'success'
          && steps.bundle.outcome != 'success'
          && steps.mark.outputs.already_claimed != 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          STATE: ${{ steps.prepare.outputs.no_op_head != '' && 'no_op' || 'gave_up' }}
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
        run: bash "${RESOLVER_DIR}/auto-resolve/status-comment.sh"

      - name: Release this head's attempt mark (this run died before it spent)
        # The mark is priced in MONEY, so a run that reached neither the model nor
        # the bundle hands it back: a crash in the toolchain install, a cancelled
        # run, a job killed at its timeout before the ladder started. Marking on
        # success only would leave the EXPENSIVE failures free to repeat, which is
        # why mark-attempt runs first — but a run that bought nothing then holds the
        # head for a floor-hour, and this repository merges to main every few
        # minutes, so the PR sits conflicted through ten base pushes for a failure
        # that cost a runner minute.
        #
        # `skipped` on the ladder AND the bundle is the whole test: a ladder that
        # ran (or was cancelled mid-flight) may have billed, and a bundle that ran
        # produced a verdict a re-run reproduces. Neither is this arm.
        #
        # The handoff step is skipped too, for the reason its sibling release above
        # gives: the unmergeable-conflict ending is a VERDICT, so re-running against
        # the same tree reaches the same wall. It exits 1 with both other steps
        # skipped, so without this clause its failure reads as a run that died
        # before it spent. `auto-resolve-blocked` normally keeps re-scanning cheap,
        # but the mark is the backstop when that label write is what failed.
        if: >-
          always()
          && (failure() || cancelled())
          && steps.mark.outputs.head_sha != ''
          && steps.mark.outputs.already_claimed != 'true'
          && steps.ladder.outcome == 'skipped'
          && steps.bundle.outcome == 'skipped'
          && steps.handoff.outcome == 'skipped'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          HEAD_SHA: ${{ steps.mark.outputs.head_sha }}
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
        run: bash "${RESOLVER_DIR}/auto-resolve/release-attempt.sh"

      - name: Stage the fan-out logs for publication
        id: stage_logs
        # The fan-out directory — every shard's result JSON and stderr — is the only
        # record of what an individual resolution actually DID, and $RUNNER_TEMP is
        # destroyed with the runner. Without this, a refusal or a wrong resolution
        # can only be reconstructed by deduction from the step log.
        #
        # always(): a refusal reds the finalize step below, but a resolution that
        # pushes something WRONG stays green — and that is the case whose transcript
        # matters most, so it cannot hang off failure().
        #
        # Ordered AFTER the bundle upload because the stager reds this job when the
        # engine is missing, and a step that fails ahead of the bundle would take a
        # paid, completed resolution down with it: a diagnostic must be loud without
        # being able to discard the work it documents.
        #
        # Every OTHER caller publishes through the ./.github/actions/upload-agent-logs
        # composite; this one deliberately does not. A local `uses: ./…` manifest is
        # read from the WORKSPACE at step time, and from the prepare step onward this
        # job's workspace is the untrusted PR head left MID-MERGE — so a PR that
        # conflicts on the composite's own action.yaml would hand the runner a
        # manifest full of conflict markers and kill this step, which is the exact
        # failure bb473538 removed from this job (and whose script header says never
        # to reintroduce). The shared part — redaction, the fail-closed placeholder —
        # still comes from the same base-staged stage-agent-logs.sh every caller runs;
        # only the two-line upload wrapper is spelled out here, against a SHA-pinned
        # remote action rather than a workspace one.
        #
        # The engine and its redactor_config likewise come from the resolver
        # checkout: masking a public artifact with config the pull request wrote is
        # no masking.
        #
        # An absent `log-redactor` publishes NOTHING rather than publishing raw. A
        # fan-out log carries whatever the merge exposed the agent to, so a caller
        # with no redactor is a caller whose logs must not leave the runner.
        if: always() && steps.prepare.outputs.needs_llm == 'true' && inputs.log-redactor != ''
        # Bounded so redaction cannot eat the rest of the job, and counted in the
        # job budget above. A tree with ~1200 log files took 19 minutes here; past
        # this cap the logs are worth less than the steps behind them, and the step
        # reds loudly rather than letting the job hit its own cap and report
        # `cancelled`.
        timeout-minutes: 5
        env:
          LOGS_PATH: ${{ steps.ladder.outputs.fanout_dir }}
          LOGS_OUT: ${{ runner.temp }}/agent-logs-upload/auto-resolve
          REDACTOR: ${{ steps.base.outputs.dir }}/${{ inputs.log-redactor }}
          REPO_ROOT: ${{ steps.base.outputs.dir }}
          PYTHON: ${{ steps.engine.outputs.python }}
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
        run: |
          mkdir -p "${LOGS_OUT}"
          bash "${RESOLVER_DIR}/stage-agent-logs.sh"

      - name: Publish the fan-out logs
        if: always() && steps.stage_logs.outcome == 'success'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: claude-logs-auto-resolve-pr${{ inputs.pr }}
          path: ${{ runner.temp }}/agent-logs-upload/auto-resolve
          if-no-files-found: ignore
          retention-days: 14

      - name: Publish this job's usage records
        # Each rung's own decider billed it as it ran, and the self-review inside
        # `bundle` appended its attempts to the same file — so this step only
        # publishes, and adding a rung needs no edit here.
        #
        # A REMOTE upload action, never a local `uses: ./…`: this job's workspace
        # is the untrusted PR head left mid-merge, where a conflicted manifest is
        # not YAML.
        #
        # The condition tests only the run's state, never the ledger's existence:
        # `hashFiles()` resolves its glob inside GITHUB_WORKSPACE, and runner.temp
        # sits outside it, so a file guard there is never true.
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          # strategy.job-index is 0 on a job with no matrix, so one naming rule
          # covers every Claude surface and a matrix instance cannot collide with
          # its siblings.
          name: claude-usage-${{ github.job }}-${{ strategy.job-index }}
          path: ${{ runner.temp }}/claude-usage.jsonl
          if-no-files-found: ignore
          overwrite: "true"
          retention-days: 14

  # ── Land the resolution. Holds every write credential; runs no model, no
  #    PR-authored install, no PR-authored script, no local composite.
  land:
    name: Land the auto-resolved merge
    needs: [resolve]
    # always(), so a resolve that FAILED still lets this job report; gated on
    # discover having selected the PR at all, since there is otherwise nothing to
    # push and no branch name to push it to. Whether there is anything to push is
    # decided by whether the artifact exists.
    if: >-
      always() && needs.resolve.result != 'skipped' &&
      needs.resolve.outputs.selected == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # NOT cancel-in-progress, unlike a scan: this job's whole body is a
    # verify-then-push, and cancelling it mid-push wastes a completed paid
    # resolution. A superseding run's push is reconciled by the optimistic lock
    # (the push is non-force) and by the stand-down check, not by cancellation.
    concurrency:
      group: auto-resolve-conflicts-land-${{ inputs.pr }}
      cancel-in-progress: false
    permissions:
      contents: write # push the merge commit to the PR head
      pull-requests: write # comment on / label the PR
      issues: write # `gh label create` for auto-resolve-blocked rides the issues API
      # land.sh re-dispatches this workflow when a concurrent push made its
      # resolution stale. The dispatch reaches nothing the pull request controls:
      # this job runs only default-branch code (see the staging step below), and
      # the run it starts re-discovers and re-resolves from scratch.
      actions: write
      # land.sh marks the head it PUSHED as attempted: that push re-triggers this
      # workflow onto the resolution it just landed, and without the mark the next
      # scan buys the same tree again. `commit_status_mark_set` ends in `|| true`,
      # so without this scope the write 403s silently.
      statuses: write
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GH_REPO: ${{ github.repository }}
      PR: ${{ inputs.pr }}
      HEAD_REF: ${{ needs.resolve.outputs.head_ref }}
      BASE_REF: ${{ needs.resolve.outputs.base_ref }}
      # The head discover selected this run for. land compares the bundle's own
      # head-side parent against it, so a branch force-pushed mid-resolution ends
      # as a discard rather than as a tamper refusal.
      HEAD_SHA: ${{ needs.resolve.outputs.head_sha }}
    steps:
      - name: Checkout the PR head
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # The PR's own content, but nothing in this job executes it: it is here
          # so git has the objects the bundle is thin against and a worktree to
          # push from. Everything this job RUNS comes from the default-branch staging
          # below.
          ref: ${{ needs.resolve.outputs.head_ref }}
          fetch-depth: 0
          persist-credentials: false

      - name: Check out the resolver from its own repository
        # THE REASON THIS IS A REUSABLE WORKFLOW AND NOT A COMPOSITE ACTION.
        # A local `uses: ./...` resolves against the WORKSPACE, which in this job is
        # the pull request's own copy left MID-MERGE. The runner would read a
        # manifest the pull request wrote, and a manifest that is itself conflicted
        # is not YAML, so every credential rung died before the resolver started.
        # This job used to answer that by staging a second worktree off the default
        # branch by hand, inline, because the bootstrap could not itself live in a
        # script it had not yet staged.
        #
        # A reusable workflow is fetched by the runner from ITS OWN repository, so
        # the resolver arrives by clone from a repository the pull request cannot
        # write. The tree being merged and the code doing the merging are now
        # different repositories, which is what the staging worktree was
        # approximating.
        id: resolver
        env:
          RESOLVER_REPO: ${{ inputs.resolver-repository }}
          RESOLVER_REF: ${{ inputs.resolver-ref }}
        run: |
          dest="${RUNNER_TEMP}/resolver"
          git clone --no-tags "https://github.com/${RESOLVER_REPO}.git" "${dest}"
          # A ref may be a branch, a tag or a commit SHA. `checkout --detach` takes
          # all three; `clone --branch` refuses a SHA, which is the form a caller
          # pinning its `uses:` line has.
          git -C "${dest}" checkout --detach "${RESOLVER_REF}"
          echo "dir=${dest}/.github/resolver" >>"$GITHUB_OUTPUT"

      - name: Download the resolved merge
        id: bundle
        # continue-on-error: no artifact means this PR's resolve produced nothing
        # to push (it failed and said so, or it found nothing to resolve). The land
        # script reports that and exits 0 rather than manufacturing a second red.
        continue-on-error: true
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: auto-resolve-merge-${{ inputs.pr }}
          path: ${{ runner.temp }}/auto-resolve-bundle

      - name: Re-verify the resolution and push it
        id: land
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          AUTOFIX_TOKEN_ORG: ${{ secrets.AUTOFIX_TOKEN_ORG }}
          # Workflow-scoped PAT, used only when the merge commit changes files
          # under .github/workflows/ — no other available token can push those.
          TEMPLATE_SYNC_TOKEN_ORG: ${{ secrets.TEMPLATE_SYNC_TOKEN_ORG }}
          BUNDLE_DIR: ${{ runner.temp }}/auto-resolve-bundle
          # True only on the retry land.sh itself dispatched, which is what stops
          # a steadily pushing author driving one paid model run per push.
          AFTER_RACE: ${{ inputs.after-race }}
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
        run: bash "${RESOLVER_DIR}/auto-resolve/land.sh"

      - name: The resolution is on the branch
        # The one step whose presence means a resolution REACHED the head.
        # land.sh exits 0 on four endings that push nothing, so this job's own
        # conclusion cannot say which happened; repo-health's
        # conflict-to-landing latency reads this step and dates the landing from
        # it (.github/scripts/_chart_ci_runs.py).
        if: steps.land.outputs.pushed == 'true'
        run: echo "the resolved merge is on the pull request head."

      - name: Say on the PR that the landing did not push
        # Covers the endings land.sh cannot report itself: a cancellation, the job
        # timeout, a crash before its own diagnosis. It rewrites nothing that
        # already carries a verdict, so a landed resolution, a land failure and a
        # resolve that gave up all keep what they published.
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          STATE: not_landed
          RESOLVER_DIR: ${{ steps.resolver.outputs.dir }}
        run: bash "${RESOLVER_DIR}/auto-resolve/status-comment.sh"
